import { SpeedValidatorDirective } from './speed-validator.directive';

describe('SpeedValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SpeedValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
